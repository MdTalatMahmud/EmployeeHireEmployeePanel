<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="au.mgemployeehire.employeehireemployeepanel">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/jobhuntlogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/jobhuntlogo"
        android:supportsRtl="true"
        android:theme="@style/Theme.EmployeeHireEmployeePanel">
        <activity android:name=".ExpressAssistantOrAdminSubMenuActivity"></activity>
        <activity android:name=".ExpressStaffSubMenuActivity" />
        <activity android:name=".ExpressJobHuntSubMenuActivity" />
        <activity android:name=".FirstMenuActivity" />
        <activity android:name=".JobPositionSelectionActivity" />
        <activity android:name=".SetMyContactDetailsActivity" />
        <activity android:name=".SigninActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignupActivity" />
        <activity android:name=".LocationActivity" />
        <activity android:name=".WhatKindsOfJobsActivity" />
        <activity android:name=".PartOrFullTimeJobActivity" />
        <activity android:name=".INeedJobActivity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".JobDetailsActivity" />
        <activity android:name=".AppliedJobsActivity" />
        <activity android:name=".JobAdvertisementActivity" />
        <activity android:name=".MainActivity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
    </application>

</manifest>